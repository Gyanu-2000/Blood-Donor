# TODO - Fix `Exception on / [GET]`

- [ ] Update `app.py` to always pass `message` to `blood.html` when rendering `/`
- [ ] Clean up `templates/blood.html` minor markup issues (remove stray trailing `>` and fix the invalid `hover` attribute)
- [x] Re-run Flask app locally and verify `/` loads without throwing an exception
- [ ] If error persists, run again with debug/traceback and fix the exact cause
